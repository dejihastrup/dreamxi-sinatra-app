<div class="container">
  <nav>
    <% if logged_in? %>
    Logged in as: <%= current_user.username %> | 
    <a href="/logout">Logout</a>
    <% else %>
    <a href="/signup">Sign Up</a> |
    <a href="/login">Login</a>
    <% end %>
  </nav>
</div>
